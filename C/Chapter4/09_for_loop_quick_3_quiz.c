#include<stdio.h>

int main(){
    int n=90;//for loop is most important
    for (int a=1; a<=n; a++){
        printf("%d\n",a);
    }
    // for (initialize; test; increment or decrement) { }
    return 0;
}