#include<stdio.h>

int main(){
    int i = 0;
    while (i < 4){       //{}A piece of code in curly blaces can be called block of code.
        printf("Hello!\n");      // This block of code runs when i remains less than 4 and then stops.
        // i = i + 1;
        i++;
    }
    return 0;
}