#include <stdio.h>

int main() {
    printf("char = %zu bytes\n", sizeof(char));//size of func is used to check size of a data type
    printf("int = %zu bytes\n", sizeof(int));
    printf("float = %zu bytes\n", sizeof(float));
    return 0;
}